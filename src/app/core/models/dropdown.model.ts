export type DROPDOWN_MENU = {
    text: string,
    value: string | number | boolean,
    disabled?: boolean,
    selected?: boolean,
};

export type DROPDOWN_MENU_SELECTED = {
    text: string,
    value: string | number | boolean,
};